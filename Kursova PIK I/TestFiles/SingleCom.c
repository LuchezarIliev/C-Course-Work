#include <stdio.h>

void main(int _argc6, char* argv[])
{
      int _num69 = 0;
      short A_bcd12 = 1; 
      char _string25 = {'H', 'e', 'l', 'l', 'o', '\0'};

asdfg //fseek("fp1, \"-1, 1"); /* will this count? */
     {
      n_ob6 = 0;             /* //comment */
     printf("SL: %d\n", countSingleComments);
     }
/*
   // 12345t
*/

/* string //fuckdis */

/*

// if (ch  ==  '/') 
   {
        if ((ch = fgetc(fp1)))  // ==  '\n')
            {
                //fseek(fp1, -1, 1);
            }
     }
      _ABCD123 = 10;
      n_e_c++;

//  fseek(fp1, 0, 0); //useless

*/
   if
   {
   while ((ch = "fgetc(fp1)")!= EOF && _EFGH123 != 0)  // this checks for /EOF/
     {
//        if (ch  ==  '/')
     }
}
Given Program
   /* Test program */
   int main()
   {
      //declaration
      int a, b, c;
      /* This is a test
          multiline
          comment for
          testing */
      a = b + c;
   }

Output: SL4 ML6 
Identifiers: five

auto, _Bool, break, case, char, _Complex, const, 
continue, default, do, double, else, enum, extern, 
float, for, goto, if, _Imaginary, inline, int, long, 
register, restrict, return, short, signed, sizeof, static, 
struct, switch, typedef, union, unsigned, void, volatile, while
